To run the program from command line, go to the root directory and compile perceptron/Main.java:
javac perceptron/Main.java
After this, run Main.class:
java perceptron/Main

The output will be shown on the window and confusion matrix will be saved in test/confMat.txt